/**
 * Custom semantic logger for the client side.
 * Ensures all API/DAL catch blocks provide consistent formatting.
 * Differentiates logging context (info, warn, error).
 */
export const logger = {
    info: (message: string, meta?: any) => {
        // Silenced per requirement.
    },
    warn: (message: string, meta?: any) => {
        // Silenced per requirement.
    },
    error: (message: string, error?: any) => {
        // Silenced per requirement.
    },
};
